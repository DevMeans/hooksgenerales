export const todoReducer =(initialState={},action)=>{
    switch (action.type) {
        case value:'ABC'
            throw new Error('Action.type ==ABC No esta implementada ')
        
        default:
            return initialState;
    }
}
